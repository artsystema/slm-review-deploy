---
deployment:
  tasks:
    - /usr/bin/test -d "$HOME/slm.artsystema.com"
    - /usr/bin/test -f "$HOME/slm.artsystema.com/app-root.php"
    - /usr/bin/test -f "$HOME/slm-review/private/config.php"
    - /bin/mkdir -p "$HOME/slm-review/app" "$HOME/slm-review/migrations" "$HOME/slm.artsystema.com/assets"
    - /bin/cp -R app/. "$HOME/slm-review/app/"
    - /bin/cp -R migrations/. "$HOME/slm-review/migrations/"
    - /bin/cp -R public/assets/. "$HOME/slm.artsystema.com/assets/"
    - /bin/cp public/index.php "$HOME/slm.artsystema.com/index.php"
